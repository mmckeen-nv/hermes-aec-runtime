# AECTransaction minimum fields

`schema_version`, `transaction_id`, `request`, `host`, `dry_run`, and `actions`. Each action contains one registered semantic `operation`, stable `target_ids`, and typed `parameters`.

