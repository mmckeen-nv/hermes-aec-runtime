# SceneIndex minimum fields

`schema_version`, `document_id`, `host`, `units`, `generated_at`, and `objects`. Each object has a stable `id`, `name`, `kind`, `layer`, and a small property map. Host adapters may add bounds, material, parent, revision, or spatial index data without changing the core contract.

