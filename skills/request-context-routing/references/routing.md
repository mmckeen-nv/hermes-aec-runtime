# Routing policy

Keep only the objects, constraints, and prior receipts needed for the current outcome. Route research only when external facts are required. Route mutation through Action Assembly. Route every mutation result through Proof and Recovery.

